# Устранение проблем

## Проблема: Ошибки при загрузке данных

### Симптомы

- Сообщение об ошибке: "Не удалось загрузить основной файл"
- Сообщение об ошибке: "Не удалось загрузить файл брака"
- Пустые графики и таблицы
- Индикатор статуса красный

### Решения

#### 1. Проверка источника данных

**Локальные файлы:**

- Убедитесь, что файлы `data.csv` и `defect.csv` находятся в той же директории, что и `index.html`
- Проверьте, что файлы не повреждены
- Откройте файлы в текстовом редакторе и проверьте формат

**Google Таблицы:**

- Убедитесь, что таблица опубликована (Файл → Поделиться → Опубликовать в веб)
- Проверьте формат ссылки (должна содержать `output=csv`)
- Убедитесь, что доступ по ссылке разрешён

#### 2. Очистка localStorage (Рекомендуется)

Если вы ранее вводили ссылки через UI, они сохранены в localStorage и могут конфликтовать.

**Способ 1: Через специальную страницу**

1. Откройте файл `clear_storage.html` в браузере
2. Нажмите кнопку "🗑️ Очистить настройки Google Sheets"
3. Перейдите обратно в приложение (кнопка "↩️ Вернуться в приложение")

**Способ 2: Через консоль браузера**

1. Откройте консоль браузера (F12)
2. Введите и выполните:

```javascript
localStorage.removeItem("googleSheetsConfig");
location.reload();
```

#### 3. Проверка ссылок Google Sheets

**Проверьте формат ссылки:**

- Ссылка должна содержать `docs.google.com/spreadsheets`
- Ссылка должна содержать `output=csv`
- Ссылка должна начинаться с `https://`

**Пример правильной ссылки:**

```
https://docs.google.com/spreadsheets/d/e/2PACX-1vQixzAsPJHQYMiHKyQILPshMPMaJSDOWQ_XKRg9mwmfBDs1pGWmgg70QrenN3SYRlAygLeMKZtLTGYq/pub?output=csv
```

**Как проверить:**

1. Скопируйте ссылку из настроек приложения
2. Откройте её в новой вкладке браузера
3. Если скачивается CSV файл — ссылка верная
4. Если открывается Google Таблица — ссылка неверная

#### 4. Публикация Google Таблицы

**Убедитесь, что таблица опубликована:**

1. Откройте Google Таблицу
2. Файл → Поделиться → Опубликовать в веб
3. Убедитесь, что включена публикация
4. Формат должен быть "Значения через запятую (.csv)"
5. Скопируйте ссылку заново

#### 5. Проверка доступа к таблице

**Убедитесь, что таблица доступна по ссылке:**

1. Откройте Google Таблицу
2. Нажмите "Поделиться"
3. Убедитесь, что выбрано "Любой, у кого есть ссылка"
4. Или проверьте, что вы вошли в Google

#### 6. Проверка через консоль браузера

**Диагностика:**

1. Откройте приложение
2. Откройте консоль браузера (F12)
3. Найдите сообщения "=== ЗАГРУЗКА ДАННЫХ ==="
4. Проверьте URL, статус ответа и размер контента

**Пример успешной загрузки:**

```
=== ЗАГРУЗКА ДАННЫХ ===
Используем Google Sheets как источник данных
Основной файл URL: https://docs.google.com/spreadsheets/d/...
Загрузка основного файла из: https://docs.google.com/spreadsheets/d/...
Статус ответа: 200 OK
Размер контента: 12345 байт
Загружено 100 записей основного файла
=== ЗАГРУЗКА ЗАВЕРШЕНА УСПЕШНО ===
```

**Пример ошибки:**

```
=== ОШИБКА ЗАГРУЗКИ ===
Статус ответа: 404 Not Found
Ошибка при загрузке данных: Не удалось загрузить основной файл: 404 Not Found
```

## Проблема: Данные загружаются, но графики пустые

### Причины

- Неверный формат CSV (не разделитель `;`)
- Отсутствуют обязательные колонки
- Некорректный формат даты
- Пустые данные после фильтрации

### Решения

#### 1. Проверьте формат CSV

**Разделитель должен быть точка с запятой (;), а не запятая (,)**

Неверно:

```csv
Дата,Заказ,№Днища,Сварщик,Длина сварных швов
01.01.2024,Заказ-001,1001,Иванов И.И.,12.5
```

Верно:

```csv
Дата;Заказ;№Днища;Сварщик;Длина сварных швов
01.01.2024;Заказ-001;1001;Иванов И.И.;12.5
```

#### 2. Проверьте обязательные колонки

**Основной файл:**

- `Дата` — обязательно
- `Длина сварных швов` — обязательно

**Файл брака:**

- `Дата выяв-ния несоответствия` — обязательно
- `Технологическая операция` — обязательно
- `Исполнитель` — обязательно

#### 3. Проверьте формат даты

**Формат должен быть DD.MM.YYYY:**

- Верно: `01.01.2024`, `15.12.2023`
- Неверно: `2024-01-01`, `01/01/2024`, `1.1.24`

#### 4. Сбросьте фильтры

Если графики пустые после применения фильтров:

1. Нажмите кнопку "⟲ Сбросить"
2. Проверьте, что данные появились

## Проблема: Настройки не сохраняются

### Причины

- Браузер блокирует localStorage
- Режим инкогнито
- Очистка данных браузером

### Решения

1. **Отключите режим инкогнито**
2. **Разрешите cookies и данные сайтов**
3. **Проверьте консоль на ошибки localStorage**
4. **Не используйте настройки приватности, блокирующие localStorage**

## Проблема: Ссылки работают в браузере, но не в приложении

### Причины

- CORS ограничения
- Google кэширует старую версию
- Блокировка загрузки скриптов

### Решения

1. **Подождите 10-15 минут** после публикации таблицы
2. **Используйте локальный сервер** вместо прямого открытия файла:

   ```bash
   python -m http.server 8000
   ```

   Затем откройте `http://localhost:8000`

3. **Попробуйте другой браузер**

4. **Проверьте консоль на ошибки CORS**

## Проблема: Экспорт не работает

### Причины

- Нет данных для экспорта
- Ошибка в браузере при скачивании файла

### Решения

1. **Проверьте, что данные загружены**
2. **Попробуйте применить фильтры перед экспортом**
3. **Проверьте консоль на ошибки**
4. **Попробуйте другой браузер**

## Проблема: Производительность низкая

### Симптомы

- Медленная загрузка данных
- Задержки при фильтрации
- Медленное обновление графиков

### Решения

1. **Уменьшите объём данных:**
   - Удалите старые записи из источника данных
   - Используйте фильтры для работы с подмножеством данных

2. **Используйте локальный сервер:**

   ```bash
   python -m http.server 8000
   ```

3. **Очистите кэш браузера**

4. **Используйте современный браузер**

## Проблема: Графики не отображаются

### Причины

- Нет данных
- Chart.js не загрузился
- Ошибка в данных

### Решения

1. **Проверьте консоль на ошибки**
2. **Убедитесь, что данные загружены** (индикатор статуса)
3. **Проверьте интернет-соединение** (для загрузки Chart.js с CDN)
4. **Проверьте формат данных** (см. выше)

## Проблема: Брак не считается правильно

### Причины

- Данные о браке недоступны
- Несоответствие названий колонок
- Несовпадение ключей связи

### Решения

1. **Проверьте источник данных:**
   - Если используется Google Sheets — проверьте публикацию листа "Брак"
   - Если используются локальные файлы — проверьте наличие defect.csv

2. **Проверьте названия колонок:**
   - `Дата выяв-ния несоответствия`
   - `Технологическая операция`
   - `Исполнитель`
   - `№ Днища, № чертежа, артикул`
   - `Номер заказа`

3. **Проверьте соответствие ключей:**
   - `Номер заказа` и `№ Днища` должны совпадать в основном файле и файле брака
   - Фамилии сварщиков должны записываться одинаково

4. **Если данные о браке недоступны:**
   - Настройте правило брака в интерфейсе
   - Выберите поле, оператор и значение
   - Нажмите "Применить правило"

## Проблема: Повторные исправления не отображаются

### Причины

- Нет записей с операцией "Исправление повторное"
- Неверное название операции

### Решения

1. **Проверьте файл брака на наличие записей:**
   - `Технологическая операция` должна быть "Исправление повторное"
   - Проверьте точное название (регистр, пробелы)

2. **Проверьте консоль на сообщения**

3. **Если нет повторных исправлений — это нормально**
   - График покажет "Нет данных" или нулевые значения

## Проблема: Прочие дефекты не отображаются

### Причины

- Все дефекты относятся к "Предъявлению продукции" или "Исправлению повторному"
- Нет записей с другими типами операций

### Решения

1. **Проверьте файл брака на наличие записей с другими операциями**
   - Например: "Входной контроль", "Межоперационный контроль" и т.д.

2. **Если нет прочих дефектов — это нормально**
   - График покажет "Нет данных"

## Полная инструкция по настройке с нуля

### Шаг 1: Подготовка данных

1. Подготовьте Google Таблицу или CSV файлы с данными
2. Убедитесь в правильности формата (разделитель `;`, формат даты DD.MM.YYYY)
3. Проверьте наличие обязательных колонок

### Шаг 2: Публикация Google Таблицы (если используете)

1. Откройте Google Таблицу
2. Файл → Поделиться → Опубликовать в веб
3. Выберите "Весь документ" или конкретный лист
4. Формат: "Значения через запятую (.csv)"
5. Нажмите "Опубликовать"
6. Скопируйте ссылку

### Шаг 3: Проверка ссылки

1. Откройте ссылку в новой вкладке
2. Убедитесь, что скачивается CSV файл
3. Откройте CSV файл и проверьте формат

### Шаг 4: Очистка старых настроек

1. Откройте `clear_storage.html`
2. Нажмите "Очистить настройки Google Sheets"
3. Вернитесь в приложение

### Шаг 5: Настройка в приложении

1. Откройте приложение
2. Разверните секцию "Настройка источников данных"
3. Выберите источник (локальные файлы или Google Таблицы)
4. Введите ссылки (если используете Google Sheets)
5. Нажмите "Применить настройки"
6. Нажмите "Перезагрузить данные"

### Шаг 6: Проверка

1. Откройте консоль (F12)
2. Найдите сообщения "=== ЗАГРУЗКА ДАННЫХ ==="
3. Убедитесь, что статус ответа 200 OK
4. Проверьте количество загруженных записей
5. Проверьте, что графики отображаются

## Контакты и поддержка

Если проблема не решена:

1. Откройте консоль браузера (F12)
2. Сделайте скриншот ошибок
3. Проверьте файл `GOOGLE_SHEETS_SETUP.md` для получения дополнительной информации
4. Проверьте файл `TESTING.md` для инструкций по тестированию
5. Обратитесь к разработчику

## Часто задаваемые вопросы

### Q: Почему ссылки не сохраняются?

A: Проверьте, что вы не в режиме инкогнито и браузер разрешает localStorage.

### Q: Можно ли использовать Google Таблицы без публикации?

A: Нет, таблица должна быть опубликована для доступа через CSV.

### Q: Как часто обновляются данные?

A: Google кэширует опубликованные таблицы. Обычно обновление занимает 5-15 минут.

### Q: Можно ли использовать локальные файлы вместо Google Sheets?

A: Да, просто очистите настройки и используйте файлы `data.csv` и `defect.csv`.

### Q: Что делать, если формат даты отличается?

A: Вам нужно преобразовать даты в формат DD.MM.YYYY в Google Таблице или CSV файле.

### Q: Почему графики показывают "Нет данных"?

A: Это может быть из-за отсутствия данных после фильтрации или отсутствия данных определённого типа (например, повторных исправлений).

### Q: Как вернуть локальные файлы после использования Google Sheets?

A: Откройте clear_storage.html и очистите настройки Google Sheets.

### Q: Что такое CORS ошибка?

A: Это ошибка безопасности браузера. Используйте локальный сервер вместо прямого открытия файла.

### Q: Можно ли использовать приложение без интернета?

A: Да, если вы используете локальные файлы и Chart.js загружен локально. В противном случае требуется интернет для загрузки библиотек.

### Q: Почему таблица показывает только 500 записей?

A: Это ограничение для производительности. Все записи используются для графиков и анализа.

---

**Примечание:** Всегда проверяйте консоль браузера (F12) для получения подробной информации об ошибках.
